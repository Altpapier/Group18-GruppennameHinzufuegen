# Group18-GruppennameHinzufuegen
Für die Seiten `index.html`, `survey.html` und `live_feedback_lecturer.html` wird CouchDB verwendet.
Das Username und Passwort ist dabei in `index.js`, `survey.js` und `liveFeedbackLecturer.js` zu ändern.
Wenn eine Instanz läuft sollte alles automatisch direkt funktionieren
